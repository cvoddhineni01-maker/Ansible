- name: loops using packages
  hosts: local
  connection: local
  become: yes
  tasks:
   - name: Print names
     ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state}}"
     loop:
      - {name: "nginx", state: "absent"}
      - {name: "mysql", state: "present"}
    
