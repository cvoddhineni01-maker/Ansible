- name: loops using packages
  hosts: local
  connection: local
  become: yes
  tasks:
   - name: Print names
     ansible.builtin.package:
      msg: "Hello {{ item }}"
      state: latest
     loop:
      - nginx
      - git
      - mysql
