
- name: variables from files
  hosts: localhost
  connection: local
  vars_files:
    - course.yaml

  tasks:
    - name: Ansible Course
      vars:
        Course: "AWS"
      ansible.builtin.debug:
        msg: "Course name is: {{ Course }}"

    - name: Ansible Course
      vars:
        Course: "AWS"
      ansible.builtin.debug:
        msg: "Course name is: {{ Course }}" 