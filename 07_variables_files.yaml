- name: varibles from files
  hosts: localhost
  connection: local
  vars_files: 
  - course.yaml
  tasks: 
    - name: Ansible Course
        Course: AWS
      ansible.builtin.debug:
        msg: "Course name is: {{ Course }}"
