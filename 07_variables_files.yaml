
- name: variables from files
  hosts: localhost
  connection: local
  vars_files:
    - course.yaml

  tasks:
    - name: Ansible Course
      vars:
        Course: "AWS"
      ansible.builtin.debug:
        msg: "Course name is: {{ Course }}"

    - name: Ansible Course
      vars:
        Course: "AWS"
      ansible.builtin.debug:
        msg: "Course name is: {{ Course }}" 

        #ansible-playbook -i inventory.ini -e ansible_user=ec2-user -e ansible_password=Devops321 07_variables_files.yaml
